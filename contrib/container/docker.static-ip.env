# InvenTree environment variables for a static IP address setup
# These variables will be used by the docker-compose.yml file
# You can customize these values for your static IP environment

# IMPORTANT: Replace 192.168.100.61 with your actual static IP address
# Example: STATIC_IP_ADDRESS=192.168.100.61
STATIC_IP_ADDRESS=192.168.100.61
INVENTREE_SITE_URL=http://${STATIC_IP_ADDRESS}:8000

# Set DEBUG to False for a production-like setup
INVENTREE_DEBUG=False
INVENTREE_LOG_LEVEL=INFO
INVENTREE_DB_LOGGING=False

# Database configuration options
# Note: The example setup is for a PostgreSQL database (change as required)
INVENTREE_DB_ENGINE=postgresql
INVENTREE_DB_NAME=inventree
INVENTREE_DB_HOST=inventree-static-ip-db
INVENTREE_DB_PORT=5432
INVENTREE_DB_USER=pguser
INVENTREE_DB_PASSWORD=pgpassword

# Enable custom plugins?
INVENTREE_PLUGINS_ENABLED=True

# Auto run migrations?
INVENTREE_AUTO_UPDATE=True

# CORS (Cross-Origin Resource Sharing) configuration
# For static IP, allow specific origins
INVENTREE_CORS_ORIGIN_ALLOW_ALL=True
INVENTREE_CORS_ALLOW_CREDENTIALS=True
# Comma-separated list of allowed CORS origins
# Uses STATIC_IP_ADDRESS variable
INVENTREE_CORS_ORIGIN_WHITELIST=http://${STATIC_IP_ADDRESS}:8000,http://${STATIC_IP_ADDRESS}:5173
# Comma-separated list of CORS origin regex patterns
# Uses STATIC_IP_ADDRESS_ESCAPED variable (dots are escaped for regex)
INVENTREE_CORS_ORIGIN_REGEX=^http://${STATIC_IP_ADDRESS_ESCAPED}:\\d+$

# ALLOWED_HOSTS configuration
# Comma-separated list of allowed hosts
# Uses STATIC_IP_ADDRESS variable
INVENTREE_ALLOWED_HOSTS=${STATIC_IP_ADDRESS},localhost,127.0.0.1, http://${STATIC_IP_ADDRESS}:5173

# CSRF Trusted Origins
# Comma-separated list of trusted origins for CSRF
# Uses STATIC_IP_ADDRESS variable
INVENTREE_TRUSTED_ORIGINS=http://${STATIC_IP_ADDRESS}:8000,http://${STATIC_IP_ADDRESS}:5173

# Admin user configuration
# These credentials will be used to create an admin user on first startup
INVENTREE_ADMIN_USER=admin
INVENTREE_ADMIN_EMAIL=admin@example.com
INVENTREE_ADMIN_PASSWORD=inventree

